name: frontendbristore
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  sentry_flutter: ^9.16.1
  flutter:
    sdk: flutter
  permission_handler: ^11.3.1
  cupertino_icons: ^1.0.8
  dio: ^5.9.2
  shared_preferences: ^2.2.3
  intl: ^0.20.2
  image_picker: ^1.2.1
  mobile_scanner: ^5.2.3
  pdf: ^3.11.0
  printing: ^5.13.0
  font_awesome_flutter: ^11.0.0
  google_fonts: ^8.0.2
  esc_pos_utils_plus: ^2.0.4
  print_bluetooth_thermal: ^1.1.6
  shimmer: ^3.0.0          # loading skeleton produk
  animations: ^2.0.0       # transisi PaymentSheet
  flutter_screenutil: ^5.9.0  # sudah kamu rencanakan
  cached_network_image: ^3.4.1
  geolocator: ^11.0.0
  geocoding: ^3.0.0

dev_dependencies:
  sentry_dart_plugin: ^3.2.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  # Untuk state management yang kamu pakai
  flutter_bloc: ^9.0.0 
  

flutter:
  uses-material-design: true
sentry:
  upload_debug_symbols: true
  upload_source_maps: true
  project: flutter
  org: ora-tau
